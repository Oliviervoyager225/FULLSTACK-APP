import React from 'react';
import '../styles/Footer.css';

export default function Footer() {
  // Footer is now integrated in the Home page as "footer-section"
  // This component is kept empty to avoid breaking imports
  return null;
}
